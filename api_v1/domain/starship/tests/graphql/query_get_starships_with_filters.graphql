query getStarships($where: StarshipWhereInput!) {
    starships(where: $where) {
        edges {
            node {
                id
                name
                length
            }
        }
    }
}
